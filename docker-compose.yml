services:
  pathcov:
    image: pathcov-image:1.0.0
    container_name: pathcov
    volumes:
      - pipeline-data:/data
      - ./configs:/configs:ro
      - ./pathcov/scripts:/scripts:ro
      - ${SUT_DIR}:/sut
    working_dir: /pathcov-project
    stdin_open: true
    tty: true

  jdart:
    image: jdart-image:1.0.0
    platform: linux/amd64
    container_name: jdart
    volumes:
      - pipeline-data:/data
      - ./configs:/configs:ro
      - ${SUT_DIR}:/sut
    working_dir: /jdart-project
    stdin_open: true
    tty: true

volumes:
  pipeline-data:
